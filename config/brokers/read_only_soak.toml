# Two hours requested; minimum qualifying soak evidence is one hour.
duration_seconds = 7200
snapshot_seconds = 60
smoke_minimum_seconds = 900
soak_minimum_seconds = 3600
heartbeat_seconds = 10
reconnect_attempts = 3
reconnect_delay_seconds = 10
cash_tolerance = "0.01"
equity_tolerance = "0.01"
clock_warning_seconds = 2
