# Runtime selection (safe Lot 0 defaults)
TRADING_AI_ENV=PAPER
TRADING_AI_PROFILE=balanced

# Broker credentials must only be supplied through the environment.
# They are unused in Lot 0 and intentionally left blank.
BROKER_API_KEY=
BROKER_API_SECRET=
BROKER_ACCOUNT_ID=
